// JavaScript me array ke elements ko filter karne ke liye kaunsa method use kiya jata hai? Example ke saath bataye.
// let arr = [1, 2, 3, 4, 4, 5];
// console.log(arr.filter(num => num>3));
// console.log(arr.filter(num => num%2===0));

// JavaScript me array ke elements ko ek specific value pe transform karne ke liye kaunsa method use kiya jata hai? Example ke saath samjhaiye.

// let arr = [1, 2, 3, 4, 5, 6];
// console.log(arr.map(ar => ar*2));

// let text = ["i love javascript"];
// console.log(text.map(tex => tex.toUpperCase()));

// let Objects = [
//    { name: "daulat singh", age: 21},
//    { name: "harsh", age: 22},
//    { name: "mayank", age: 23},
// ]
// console.log(Objects.map(obj => obj.name));

// JavaScript me array ke elements ko ek single value me reduce karne ke liye kaunsa method use kiya jata hai? Example ke saath bataye.

// let arr = [1, 2, 3, 4, 5, 6];
// console.log(arr.reduce(""));

// JavaScript me array ke elements ko ek single value me reduce karne ke liye kaunsa method use kiya jata hai? Example ke saath bataye.

// let arr = [1, 2, 3, 4, 5, 6];
// console.log(arr.reduce((acc,curr)=> acc + curr, 0));
// console.log(arr.reduce((acc,curr)=> acc * curr, 1));

// let arr2 = [[1,3], [2,4], [5,6]];
// console.log(arr2.reduce((acc,curr) => acc.concat(curr),[]));


// JavaScript me array ke elements ko iterate karne aur har element pe ek function apply karne ke liye kaunsa method use kiya jata hai? Example ke saath samjhaiye.

// let arr = [1, 2, 3, 4, 5, 6];
// arr.forEach((Element,index,Array)=>{
//     console.log(index+" index no "+ [Element]);
// })


// JavaScript me array ke elements ko ek specific condition ke basis pe check karne ke liye kaunsa method use kiya jata hai? Example ke saath samjhaiye.

// let arr = [1, 2, 3, 4, 5, 6];
// console.log(arr.some(num => num%2 === 0));
// console.log(arr.every(num => num%2 === 0));
